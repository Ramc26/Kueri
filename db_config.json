{
    "selection_guidelines": {
      "step_1": "Read the user's question carefully and identify the main subject/entity being queried",
      "step_2": "Check which database's keywords match the subject in the query",
      "step_3": "If the query mentions 'orders', 'products', 'customers', 'users', or 'sales', use ecom_db",
      "step_4": "If the query mentions 'employees', 'projects', 'tasks', or 'assignments', use project_db",
      "step_5": "When in doubt, check both database schemas and pick the one with relevant tables"
  },
  "databases_directory": "databases",
  "default_settings": {
    "max_query_timeout": 30,
    "enable_query_logging": true,
    "max_results_display": 1000
    }
  }
  